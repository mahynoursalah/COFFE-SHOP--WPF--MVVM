<Window x:Class="WpfApp1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp1" xmlns:Control="clr-namespace:WpfApp1.Controls" xmlns:Control1="clr-namespace:WpfApp1.CustomerView" 
        xmlns:viewmodel="clr-namespace:WpfApp1.ViewModel" 
        d:DataContext="{d:DesignInstance Type=viewmodel:MainViewModel}"
        xmlns:view="clr-namespace:WpfApp1.CustomerView"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <DataTemplate DataType="{x:Type viewmodel:CustomersViewModel}">
            <view:CustomerView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodel:ProductsViewModel}">
            <view:Products/>
        </DataTemplate>


    </Window.Resources>
   
    <Grid>


        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <!--Main Menu-->
        <Menu FontSize="20" Grid.Row="0" Background="{StaticResource BackgroundBrush}">
            <MenuItem Header="_view">
                <MenuItem Header="_Customers" Command="{Binding selectviewmodelcommand}" CommandParameter="{Binding _CustomersViewModel}"/>
                <MenuItem Header="_Products"  Command="{Binding selectviewmodelcommand}" CommandParameter="{Binding _ProductsViewModel}"/>
            </MenuItem>
        </Menu>

        <!--Header-->
        <Control:HeaderControl Grid.Row="1"/>

        <!--Customers List-->
        <ContentControl  Grid.Row="2" Content="{Binding SelectedViewModel}"/>
        


        <!--status bar-->
        <StatusBar Grid.Row="3" >
            <StatusBarItem>
                <TextBlock Text="(c) Wired brain coffee"/>
            </StatusBarItem>
        </StatusBar>


    </Grid>
</Window>
